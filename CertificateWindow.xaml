<Window x:Class="MinsPDFViewer.CertificateWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="인증서 선택" Height="450" Width="400" 
        WindowStartupLocation="CenterOwner" ResizeMode="NoResize">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="전자서명에 사용할 인증서를 선택하세요." FontWeight="Bold" Margin="0,0,0,10"/>

        <ListBox Grid.Row="1" Name="LbCertificates" Margin="0,0,0,10" SelectionChanged="LbCertificates_SelectionChanged">
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <StackPanel Margin="5">
                        <TextBlock Text="{Binding Subject}" FontWeight="Bold" FontSize="14"/>
                        <TextBlock Text="{Binding Issuer, StringFormat='발급자: {0}'}" FontSize="12" Foreground="Gray"/>
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="{Binding NotAfter, StringFormat='만료일: {0:yyyy-MM-dd}'}" FontSize="12" Foreground="Gray"/>
                            <TextBlock Text=" [만료됨]" Foreground="Red" Visibility="{Binding IsExpired, Converter={StaticResource BooleanToVisibilityConverter}}" Margin="5,0,0,0"/>
                        </StackPanel>
                        <TextBlock Text="{Binding DirectoryPath}" FontSize="10" Foreground="LightGray" TextTrimming="CharacterEllipsis"/>
                    </StackPanel>
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>

        <Button Grid.Row="2" Name="BtnBrowse" Content="📂 다른 위치에서 찾기 (USB 등)..." Click="BtnBrowse_Click" Margin="0,0,0,15" Padding="5"/>

        <Grid Grid.Row="3" Margin="0,0,0,15">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <TextBlock Text="인증서 암호:" VerticalAlignment="Center" Margin="0,0,10,0"/>
            <PasswordBox Grid.Column="1" Name="TxtPassword" PasswordChar="●" KeyDown="TxtPassword_KeyDown"/>
        </Grid>

        <StackPanel Grid.Row="4" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Name="BtnConfirm" Content="확인" Width="80" Height="30" Margin="0,0,10,0" Click="BtnConfirm_Click" IsEnabled="False"/>
            <Button Name="BtnCancel" Content="취소" Width="80" Height="30" Click="BtnCancel_Click"/>
        </StackPanel>
    </Grid>
</Window>